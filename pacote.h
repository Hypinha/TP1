#pragma once

//prototipos das funções

unsigned int empacotar(unsigned int, unsigned int, unsigned int, unsigned int, unsigned int);			//função para empacotar os dados recebidos do jogo
unsigned int funcpasso(unsigned int);																	//função para desempacotar o passo
unsigned int funccor(unsigned int);																		//função para desempacotar a cor
unsigned int funcpos(unsigned int);																		//função para desempacotar a posição
unsigned int funcvel(unsigned int);																		//função para desempacotar a velocidade
unsigned int funcpista(unsigned int);																	//função para desempacotar o estado da pista



